Thank you for deciding to install this game.
You can run the game as an .exe file or use an interpreter (Pycharm etc.),
but first you need to install the necessary libraries such as pygame and pygame-menu.

pip install pygame
pip install pygame-menu